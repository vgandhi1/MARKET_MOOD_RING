# Moved

PostgreSQL credential fixes are in **[TROUBLESHOOTING.md](../TROUBLESHOOTING.md)** → *Database Issues* → *Cannot Connect to PostgreSQL*.

Quick reset: `docker-compose down -v` then `./start_data_pipeline.sh` (deletes data).
