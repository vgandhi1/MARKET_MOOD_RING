# Market Mood Ring - Environment Variables Template
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control (it's in .gitignore)

# ============================================
# PHASE 1: ETL Pipeline (Required)
# ============================================

# Finnhub API Key (Required)
# Get your free API key from: https://finnhub.io/
FINNHUB_API_KEY=your_finnhub_api_key_here

# Optional: Custom Stock Symbols (comma-separated)
# If not set, uses default 30 tickers from producer/tickers.json
# Example: STOCK_SYMBOLS=AAPL,MSFT,TSLA,GOOGL,AMZN
# STOCK_SYMBOLS=

# ============================================
# PHASE 2: LLM Integration (Optional)
# ============================================

# LLM Provider Selection
# Options: openai, anthropic, google, huggingface, azure, ollama
# Default: ollama (if Ollama service is enabled)
LLM_PROVIDER=ollama

# --- OpenAI (if LLM_PROVIDER=openai) ---
# Get API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key-here

# --- Anthropic Claude (if LLM_PROVIDER=anthropic) ---
# Get API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# --- Google Gemini (if LLM_PROVIDER=google) ---
# Get API key from: https://makersuite.google.com/app/apikey
# GOOGLE_API_KEY=your-google-api-key-here

# --- Hugging Face (if LLM_PROVIDER=huggingface) ---
# Get API key from: https://huggingface.co/settings/tokens
# HUGGINGFACE_API_KEY=hf_your-huggingface-api-key-here

# --- Azure OpenAI (if LLM_PROVIDER=azure) ---
# Get credentials from Azure Portal
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-azure-api-key-here
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# --- Ollama (if LLM_PROVIDER=ollama) ---
# No API key needed - runs locally in Docker container
# OLLAMA_BASE_URL=http://ollama:11434  # Default, usually no need to change

# ============================================
# Notes
# ============================================
# 1. Phase 1 only requires FINNHUB_API_KEY
# 2. Phase 2 requires LLM_PROVIDER and corresponding API key
# 3. For Ollama (local), no API key needed
# 4. Never commit .env file to version control
# 5. See docs/setup/ENV_FILE_GUIDE.md for detailed LLM setup
